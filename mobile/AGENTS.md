# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This app targets **Expo SDK 54** (so it runs in the store build of Expo Go on the team's devices).
Do not bump the SDK without checking every native dependency and the Expo Go version installed on
test phones.
